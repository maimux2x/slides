<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Slides - maimux2x</title>
  <meta property="og:title" content="Slides - maimux2x">
  <meta property="og:type" content="website">
  <meta property="og:url" content="<%= h(base_url) %>/">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      background: #1a1a2e;
      color: #e0e0e0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      min-height: 100vh;
    }

    header {
      background: #16213e;
      padding: 24px 20px;
      border-bottom: 1px solid #0f3460;
      text-align: center;
    }

    header h1 {
      font-size: 24px;
      font-weight: 700;
      color: #e0e0e0;
    }

    .grid {
      max-width: 1200px;
      margin: 32px auto;
      padding: 0 20px;
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 24px;
    }

    .card {
      background: #16213e;
      border-radius: 8px;
      overflow: hidden;
      border: 1px solid #0f3460;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
    }

    .card a {
      text-decoration: none;
      color: inherit;
      display: block;
    }

    .card-thumbnail {
      width: 100%;
      aspect-ratio: 16 / 9;
      object-fit: cover;
      display: block;
      background: #0f3460;
    }

    .card-body {
      padding: 16px;
    }

    .card-title {
      font-size: 16px;
      font-weight: 600;
      color: #e0e0e0;
      margin-bottom: 4px;
      line-height: 1.4;
    }

    .card-description {
      font-size: 13px;
      color: #a8b2d1;
      line-height: 1.4;
    }

    .card-meta {
      font-size: 12px;
      color: #6a7394;
      margin-top: 8px;
    }

    @media (max-width: 600px) {
      .grid {
        grid-template-columns: 1fr;
        gap: 16px;
        padding: 0 12px;
        margin: 16px auto;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>Slides</h1>
  </header>

  <div class="grid">
    <%- slides.each do |slide| -%>
    <div class="card">
      <a href="<%= u(slide[:folder]) %>/">
        <img class="card-thumbnail" src="<%= u(slide[:folder]) %>/ogp.png" alt="<%= h(slide[:title]) %>" loading="lazy">
        <div class="card-body">
          <div class="card-title"><%= h(slide[:title]) %></div>
          <%- if slide[:description] && !slide[:description].empty? -%>
          <div class="card-description"><%= h(slide[:description]) %></div>
          <%- end -%>
          <div class="card-meta"><%= slide[:page_count] %> pages</div>
        </div>
      </a>
    </div>
    <%- end -%>
  </div>
</body>
</html>
